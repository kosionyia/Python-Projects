[project]
name = "estate-dues-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.txt"
authors = [
    { name = "kosionyia", email = "onyiakosisochi@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
estate-dues-project = "estate_dues_project:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
